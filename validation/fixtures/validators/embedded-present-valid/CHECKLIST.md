# Verification Checklist (fixture stand-in)

Empty checklist file referenced by `governance.yaml` solely so the
embedded-profile fail-closed gate can verify the path resolves to a
real file. Contents are deliberately minimal.
