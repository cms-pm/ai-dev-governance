# RTK Release Evidence Bundle — v0.6.0

- Captured: 2026-04-19
- Scope: SCN-1.4 (RTK evidence scaffolding)
- Wrapper: `.rtk/rtk` (repo-local; sets `RTK_DB_PATH=.rtk/history.db`)
- Gitignored: `.rtk/history.db*`
- Binary provenance:
  - `which rtk` → `/usr/local/bin/rtk`
  - `rtk --version` → `rtk 0.36.0`
  - Confirmed not reachingforthejack/rtk (name-collision guard per
    `~/.claude/RTK.md`); `rtk gain` and `rtk discover` both resolve.

## `rtk --version`

```
rtk 0.36.0
```

## `rtk gain` (pre-seed)

```
No tracking data yet.
Run some rtk commands to start tracking savings.
```

## `rtk discover`

```
RTK Discover -- Savings Opportunities
====================================================
Scanned: 1 sessions (last 30 days), 70 Bash commands
Already using RTK: 3 commands (4%)

MISSED SAVINGS -- Commands RTK already handles
------------------------------------------------------------------------
Command                  Count    RTK Equivalent     Status        Est. Savings
git add                     14    rtk git            existing      ~1.4K tokens
ls /Users/cms/proj/ag..     11    rtk ls             existing      ~910 tokens
cat astaire/pyproject..      3    rtk read           existing      ~375 tokens
------------------------------------------------------------------------
Total: 28 commands -> ~2.7K tokens saveable

TOP UNHANDLED COMMANDS -- open an issue?
----------------------------------------------------
Command                  Count    Example
git config                   8    git config user.name
git mv                       4    git mv BM-007.md simon-willison.md
git submodule                3    git submodule add https://github.com/c..
git tag                      2    git tag -l
git checkout                 1    git checkout v1.0.0 2>&1
git describe                 1    git describe --tags HEAD 2>/dev/null
git rev-parse                1    git rev-parse HEAD
----------------------------------------------------
-> github.com/rtk-ai/rtk/issues
```

## `rtk gain --history` (post-seed)

After running `./.rtk/rtk ls -la docs/planning/board`:

```
RTK Token Savings (Global Scope)
Total commands:    1
Input tokens:      108
Output tokens:     40
Tokens saved:      68 (63.0%)
Total exec time:   7ms (avg 7ms)
Efficiency meter: ███████████████░░░░░░░░░ 63.0%

By Command
  1.  rtk ls -la docs/plann...      1     68   63.0%     7ms  ██████████

Recent Commands
  04-19 04:42 ■ rtk ls -la docs/planni... -63% (68)
```

## Acceptance Checks (SCN-1.4-01)

- [x] `.rtk/history.db` is gitignored (see `.gitignore`).
- [x] Evidence bundle contains `rtk --version`, `rtk gain`, `rtk discover` outputs.
- [x] `rtk gain --history` returns at least one row after a rewritten command.
- [x] Binary resolves to `rtk-ai/rtk` (not the Rust Type Kit collision).

## Notes for Phase 2

`rtk discover` surfaces `git add`, `ls`, `cat` as missed-opportunity
commands. As Phase 2 Astaire bootstrap begins, agent sessions in this repo
should route through `.rtk/rtk` (or the global `rtk` proxy hook) so these
savings accrue into `docs/releases/rtk/` at release time.
